{
  "name": "guild-di-typescript",
  "private": true,
  "dependencies": {
    "reflect-metadata": "^0.1",
    "tsyringe": "^4.7",
    "typescript": "^4.9"
  },
  "devDependencies": {
    "@types/jest": "^29.4",
    "jest": "^29.4",
    "ts-jest": "^29.0"
  },
  "scripts": {
    "test": "jest"
  },
  "jest": {
    "preset": "ts-jest",
    "setupFilesAfterEnv": [
      "<rootDir>/tests/global-setup.ts"
    ],
    "moduleNameMapper": {
      "@/(.*)": "<rootDir>/src/$1"
    }
  }
}
